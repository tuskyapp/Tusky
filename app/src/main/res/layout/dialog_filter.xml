<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="16dp">

    <EditText
        android:id="@+id/phraseEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingEnd="24dp"
        android:paddingStart="24dp"
        android:hint="@string/filter_add_description"
        app:layout_constraintTop_toTopOf="parent"
        />
        <!-- Mimic AlertDialog positive/negative/neutral layout, but with our padding -->
        <Button
            android:id="@+id/filterCancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@android:string/cancel"
            android:layout_margin="4dp"
            app:layout_constraintTop_toBottomOf="@id/phraseEditText"
            app:layout_constraintLeft_toLeftOf="parent"
            />
        <Button
            android:id="@+id/filterRemove"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/filter_dialog_remove_button"
            android:layout_margin="4dp"
            app:layout_constraintTop_toBottomOf="@id/phraseEditText"
            app:layout_constraintRight_toLeftOf="@+id/filterOk"
            />
        <Button
            android:id="@+id/filterOk"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/filter_dialog_update_button"
            android:layout_margin="4dp"
            app:layout_constraintTop_toBottomOf="@id/phraseEditText"
            app:layout_constraintRight_toRightOf="parent"
            />
</androidx.constraintlayout.widget.ConstraintLayout>